# 车齿刀设计软件 - 开发上下文与指令 (Prompt Context for Claude Code)

## 1. 项目概况
本项目是一个基于 **Electron + Vue3 + Python** 的桌面端应用程序，旨在实现**车齿刀** 的参数化设计与 3D 可视化。

*   **核心功能**：用户输入齿轮参数 -> 后端调用几何内核计算 -> 生成 3D 刀具模型 -> 前端实时渲染与交互。
*   **当前阶段**：本地开发阶段，遵循 Local-First 策略，未来计划部署至云端。

## 2. 技术栈
请严格遵守以下技术栈，不要擅自更改：

### 前端与桌面端
*   **框架**：Vue 3 (必须使用 **Composition API** + `<script setup>`)
*   **语言**：TypeScript (必须开启严格模式)
*   **构建工具**：Vite
*   **桌面框架**：Electron (最新版)
*   **3D 引擎**：Three.js
*   **UI 组件库**：Element Plus (推荐)

### 后端与计算
*   **语言**：Python 3.9+
*   **Web 框架**：Flask 或 FastAPI
*   **几何内核**：OpenCASCADE Technology (OCCT)，通过 `pythonocc-core` 调用。
*   **进程管理**：Electron 主进程负责启动/停止 Python 子进程。

## 3. 系统架构与通信流程

**通信规则：**
1.  **前后端交互**：优先使用 **HTTP 请求**，避免在前端直接依赖 Node.js 的 `fs` 模块，以便未来无缝迁移至 Web 端。
2.  **原生功能**：文件读写、系统对话框通过 **IPC (Inter-Process Communication)** 调用 Electron 主进程 API。
3.  **安全性**：渲染进程禁用 `nodeIntegration`，通过 `preload.js` 暴露受控 API。

## 4. 开发规范与约束

### 4.1 配置管理
*   **严禁硬编码 IP 或端口**。
*   所有环境配置必须通过 **Vite 环境变量** (`import.meta.env.VITE_...`) 管理。
*   开发环境配置在 `.env.development`，生产环境配置在 `.env.production`。

### 4.2 代码风格
*   **Vue 组件**：使用 **SFC (单文件组件)**，样式使用 `<style scoped>`。
*   **TypeScript**：所有函数参数、返回值必须显式定义类型。
*   **异步操作**：统一使用 `async/await`，配合 `try/catch` 捕获错误。

### 4.3 3D 开发
*   **模型格式**：后端 OCCT 生成的模型应导出为 **glTF/GLB** 格式，确保传输效率。
*   **性能**：避免在 Three.js 渲染循环中创建几何体，注意资源释放。

### 4.4 后端规范
*   **CORS**：必须开启 CORS 支持 (使用 `flask-cors`)。
*   **错误处理**：捕获 OCCT 异常，返回标准 JSON 错误信息（如 `{ "error": "参数越界", "code": 400 }`）。

## 5. 目录结构标准

请参考以下结构生成文件：
my-skiving-tool/
├── .env.development # 开发环境变量
├── .env.production # 生产环境变量
├── electron/ # Electron 主进程
│ ├── main.js
│ ├── preload.js
│ └── python-manager.js
├── src/ # 前端源码
│ ├── api/ # API 封装
│ ├── assets/ # 静态资源
│ ├── components/ # Vue 组件
│ │ ├── ToolParams.vue # 参数输入
│ │ └── ModelViewer.vue # 3D 展示
│ ├── App.vue
│ └── main.ts
├── backend/ # Python 后端
│ ├── app.py
│ ├── requirements.txt
│ └── core/ # OCCT 计算逻辑
├── package.json
└── vite.config.ts

## 6. 关键业务逻辑

*   **参数**：模数、齿数、压力角、螺旋角、变位系数、前角/后角。
*   **流程**：参数 -> 离散点 -> 曲线 -> 拉伸/扫掠 -> 布尔运算 -> 导出 glTF。

## 7. 给 Claude 的指令

*   **代码生成**：先确认路径和依赖。
*   **解释**：结合 Vue 响应式原理和 OCCT 几何流程讲解。
*   **调试**：优先检查网络请求、环境变量和 TS 类型。
*   **优化**：关注 Electron 内存、Three.js 帧率和 Python 计算耗时。

## 8.  推荐的开发路线图
Hello World 阶段：
搭建 Electron + Vue3 脚手架。
Python 写一个简单的接口，生成一个立方体，用 OCCT 转成 glTF 返回给前端，Three.js 渲染出来。打通链路。
算法验证阶段：
在 Python (配合 Notebook) 中先把车齿刀的数学公式（渐开线、螺旋线、修形计算）跑通，并生成正确的 OCCT 拓扑。不要急着写 UI。
核心功能阶段：
前端设计参数表单。
后端接收参数 -> 调用算法 -> 生成 glTF -> 返回前端。
前端实现模型的旋转、缩放、剖切查看。
高级功能阶段：
添加“渲染质量”设置（低模/高模切换）。
添加颜色编码（根据曲率着色，反映切削刃的平滑度）。

---
**请根据以上上下文，协助我完成具体开发任务。**

